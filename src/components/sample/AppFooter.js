import React from 'react'
import { FooterTab, Button, Icon } from 'native-base'

const AppFooter = () => (
  <FooterTab>
    <Button transparent>
      <Icon name="ios-apps" />
    </Button>
  </FooterTab>
)

export default AppFooter
